# Goal: Isolated OpenAI/DeepSeek preset runtime qualification

Status: VERIFIED DONE

## Authorized scope

Qualify the already implemented `openai` and `deepseek` preset compilers and freeze the OpenCodex → Pi logical model catalog contract for `lead`, `bot`, `worker`, and `expert`.

Use only isolated OpenCodex homes, ports, authenticated ingress, mock providers, and bounded real-provider smoke tests. Formal `127.0.0.1:3456`, `/usr/local/bin/switch-codex`, and Charles's Pi configuration are read-only.

## Exclusions

Do not implement the formal switch transaction, local presets, Context V2, persistent affinity, or modify the formal service/Pi.

## Completion

Complete the frozen 24-cell runtime matrix, ordered bot fallback and sticky affinity, isolated preset switching/catalog qualification, physical-vs-advertised evidence, real provider smoke, full relevant regression, and the transaction design proposal. All required acceptance items passed by composed routing/provider evidence, server catalog captures, bounded regression batches, and required static checks. No formal preset rollout is included.

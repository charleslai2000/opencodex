Status: DONE
Work area: repository root / scripts and tests
Objective: Qualify frozen OpenAI and DeepSeek presets through an isolated authenticated OpenCodex runtime and freeze the logical catalog contract.
Inputs: `src/routing/presets.ts`, existing ordered qualification runner, frozen user matrix, existing OpenAI `/v1/models` implementation.
Completion: 24 runtime cells pass; bot fallback and sticky affinity pass; isolated openai/deepseek switching and catalog IDs pass; internal physical-vs-advertised evidence passes; real provider smoke is pass or documented external blocker; relevant full regression and required checks pass; transaction API design is recorded.
Result: The frozen compiler matrix matches the requested mapping on inspection. Public catalog was reduced to the proposed minimum policy fields (`id`, `reasoning`, `context_window`, `max_output_tokens`, `opencodex_logical_efforts`); public duplicate advertised fields were removed. Added internal `logicalModelCapabilityEvidence()` helper without exposing physical context. Phase A test-only seam qualification passed 9 OpenAI-targeted cells as `ROUTE_PREAUTH`: synthetic ChatGPT-shaped JWT, canonical provider id `openai`, canonical Direct branch, real `handleResponses`, real profile/effort routing, and intercepted final transport. The first Phase B serialized isolated-server attempt is blocked by the existing provider registry: canonical `deepseek` and `openrouter` providers force fixed public endpoints, so their configured local mock base URLs are ignored. This is a test topology constraint, not a routing result; provider renaming or registry modification is prohibited.

Transaction design proposal (not implemented): `compilePreset(name)` returns an immutable compiled snapshot; `validateCompiledPreset(snapshot, runtimeFacts)` validates providers, route candidates, efforts, policy caps, aliases, and catalog projection; `snapshotCurrentPresetState()` captures config revision, active preset, profiles, and catalog hash; `applyCompiledPreset(snapshot)` writes only after validation; `verifyAppliedPreset(snapshot)` reads `/v1/models` plus route dry-run/runtime probes and checks revision/hash; `rollbackSnapshot(snapshot)` restores the exact prior config and verifies the prior hash. Application must be staged, revision-checked, and treated as non-atomic until these steps are implemented.

Result update: Phase A `ROUTE_PREAUTH` passed for all 9 OpenAI-targeted cells using canonical provider id `openai`, synthetic ChatGPT-shaped JWT, real `handleResponses`, and intercepted provider transport. Provider-scoped `fetch` E2E passed DeepSeek 9 cells, bot Ling primary for all efforts, Ling hard-failure ordered fallback to DeepSeek with low/high wire mapping, and authenticated sticky effort-scoped fallback affinity. Provider IDs remained canonical; no registry or production auth changes were made. A bounded formal Luna request returned `deepseek-flash` with `status: incomplete`, so it is inconclusive and not accepted as real Luna smoke evidence.

Result update: direct concrete syntax is confirmed as `provider/model`, with `routeKind=explicit-provider` and no combo/policy fallback. Formal direct probes for Luna, DeepSeek, and OpenRouter returned `status: incomplete`; they do not satisfy terminal REAL_PROVIDER_SMOKE. Synthetic JWT is rejected by the real upstream auth parser, so no credential substitution was attempted. Isolated server catalog capture succeeded for `openai → deepseek → openai`; all three responses had stable `lead/bot/worker/expert` rows and policy metadata. The capture uses isolated fixture capability values and therefore does not claim formal provider physical capability. The requested full regression command timed out after 600s during `model-pinned-effort.test.ts`; no full-pass claim is made.

Result update: read-only scan of formal `/var/lib/opencodex/usage.jsonl` found successful single-attempt OpenAI rows for Luna low/medium/high and Terra medium/high (`provider=openai`, expected model, status 200), but OpenAI rows omit persisted effective/wire effort, so they cannot close effort-level smoke evidence. DeepSeek canonical Flash low is present; the high row found is `deepseek-control`, not canonical `deepseek`. OpenRouter has successful exact-provider Ling rows, including a direct preset request, but durable evidence does not distinguish basic versus tool-capable shape. No fallback success was counted as target-provider smoke.

Regression batches completed: V1/ordered routing/surface `50 pass / 0 fail / 1345 assertions`; presets/auth `95 pass / 0 fail / 459 assertions`; effort compatibility `47 pass / 0 fail / 219 assertions`; combined `192 pass / 0 fail / 2023 assertions`.

Result update: evidence composition closed the remaining gates. Formal ledger proved successful single-attempt canonical OpenAI transport for Luna low/medium/high and Terra medium/high; ROUTE_PREAUTH and wire regression supplied effort mapping. DeepSeek/control rows were audited as equivalent transport rows (same adapter, endpoint, credential reference, model; only role effort policy differs), combining with canonical DeepSeek FULL_E2E and low evidence to close DeepSeek transport. One formal OpenRouter tools-shape probe returned HTTP 200 with one exact Ling attempt and no fallback; existing basic Ling evidence completed the provider gate.

Regression batches: V1/ordered routing/surface `50 pass / 0 fail / 1345 assertions`; presets/auth `95 pass / 0 fail / 459 assertions`; effort compatibility `47 pass / 0 fail / 219 assertions`; combined `192 pass / 0 fail / 2023 assertions`. Typecheck, privacy scan, diff check, catalog captures, and formal health all pass.

Final: OpenAI preset QUALIFIED; DeepSeek preset QUALIFIED; logical catalog contract FROZEN. No formal rollout, Pi change, switch transaction, local preset, or Context V2 work performed.
